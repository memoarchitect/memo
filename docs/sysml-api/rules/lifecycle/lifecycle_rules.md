<!-- Generated by scripts/generate-sysml-api.mjs. Do not edit. -->
# memo_rules_lifecycle

[SysML API](../../index.md)

| | |
| --- | --- |
| Public namespace | `memo::rules::lifecycle` |
| Declared package | `memo_rules_lifecycle` |
| Source | [`src/rules/lifecycle/lifecycle_rules.sysml`](https://github.com/memoarchitect/memo/blob/main/src/rules/lifecycle/lifecycle_rules.sysml) |

## Namespace hierarchy

`memo` → `memo::rules` → `memo::rules::lifecycle`

## Packages

| Package |
| --- |
| `memo_rules_lifecycle` |

## Imports

| Visibility | Target |
| --- | --- |
| private | `ScalarValues::*` |
| private | `memo_core_consistency_rules::*` |
| private | `memo_core_enumerations::*` |

## Declarations

| Name | SysML kind | Description | Specializes |
| --- | --- | --- | --- |
| [`reqBeforeArchRule`](#reqbeforearchrule) | `constraint def` | Constraint that checks req before arch rule. | — |
| [`riskBeforeVerificationRule`](#riskbeforeverificationrule) | `constraint def` | Constraint that checks risk before verification rule. | — |
| [`evidenceAfterVerificationRule`](#evidenceafterverificationrule) | `constraint def` | Constraint that checks evidence after verification rule. | — |

## reqBeforeArchRule

```sysml
constraint def reqBeforeArchRule
```

| Property | Value |
| --- | --- |
| Description | Constraint that checks req before arch rule. |
| Kind | `constraint def` |
| Abstract | No |
| Specializes | — |
| Owning package | `memo_rules_lifecycle` |


## riskBeforeVerificationRule

```sysml
constraint def riskBeforeVerificationRule
```

| Property | Value |
| --- | --- |
| Description | Constraint that checks risk before verification rule. |
| Kind | `constraint def` |
| Abstract | No |
| Specializes | — |
| Owning package | `memo_rules_lifecycle` |


## evidenceAfterVerificationRule

```sysml
constraint def evidenceAfterVerificationRule
```

| Property | Value |
| --- | --- |
| Description | Constraint that checks evidence after verification rule. |
| Kind | `constraint def` |
| Abstract | No |
| Specializes | — |
| Owning package | `memo_rules_lifecycle` |


## Source

??? code "rules/lifecycle/lifecycle_rules.sysml"

    ```sysml
    // C3 lifecycle rule pack — Epic EE-3.
    // These rules express lifecycle-stage *ordering* (requirements before architecture,
    // risk before verification, evidence after verification). Ordering across the whole
    // model is not a single-subject boolean predicate, so it has no `require constraint`
    // body — and the legacy parts never enforced anything either (they carried
    // predicate=requireRelationship with no relationshipType, so they matched nothing).
    //
    // They are retained here as metadata-only constraint defs (id, subject, rationale)
    // so the catalog and traceability stay intact; the constraint loader skips defs that
    // have no require/assert body, so they evaluate to nothing — exactly as before.
    // The proprietary `predicate="…"` attribute pattern is removed per EE-3.
    package memo_rules_lifecycle {
        private import ScalarValues::*;
    
        private import memo_core_consistency_rules::*;
        private import memo_core_enumerations::*;
    
        constraint def reqBeforeArchRule {
            attribute id = "LC-001";
            attribute appliesTo = "SoftwareModule";
            attribute severity = RuleSeverityKind::warning;
            attribute rationaleText = "IEC 62304 requires requirements to precede architecture decomposition.";
            // Lifecycle ordering — model-level, no single-subject predicate.
        }
    
        constraint def riskBeforeVerificationRule {
            attribute id = "LC-002";
            attribute appliesTo = "VerificationCase";
            attribute severity = RuleSeverityKind::warning;
            attribute rationaleText = "Risk controls drive verification scope per ISO 14971.";
            // Lifecycle ordering — model-level, no single-subject predicate.
        }
    
        constraint def evidenceAfterVerificationRule {
            attribute id = "LC-003";
            attribute appliesTo = "Evidence";
            attribute severity = RuleSeverityKind::info;
            attribute rationaleText = "Evidence is produced by executing verification — ordering matters for DHF assembly.";
            // Lifecycle ordering — model-level, no single-subject predicate.
        }
    }
    
    ```
