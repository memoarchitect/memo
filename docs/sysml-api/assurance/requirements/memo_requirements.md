<!-- Generated by scripts/generate-sysml-api.mjs. Do not edit. -->
# memo_assurance_requirements

[SysML API](../../index.md)

| | |
| --- | --- |
| Public namespace | `memo::assurance::requirements` |
| Declared package | `memo_assurance_requirements` |
| Source | [`src/assurance/requirements/memo_requirements.sysml`](https://github.com/memoarchitect/memo/blob/main/src/assurance/requirements/memo_requirements.sysml) |

## Namespace hierarchy

`memo` → `memo::assurance` → `memo::assurance::requirements`

## Packages

| Package |
| --- |
| `memo_assurance_requirements` |

## Imports

| Visibility | Target |
| --- | --- |
| private | `ScalarValues::*` |
| private | `memo_core_common::*` |
| private | `memo_core_enumerations::*` |
| private | `memo_assurance_needs::*` |

## Declarations

| Name | SysML kind | Description | Specializes |
| --- | --- | --- | --- |
| [`RiskDriver`](#riskdriver) | `part def` | Risk driver definition specializing `RequirementDriver`. | `RequirementDriver` |
| [`IntendedUse`](#intendeduse) | `part def` | ISO 14971 risk-analysis inputs. These are model elements so hazards, scenarios, requirements, and DHF records can trace to the exact use definition or foreseeable misuse that motivated them. | `DocumentedElement` |
| [`ReasonablyForeseeableMisuse`](#reasonablyforeseeablemisuse) | `part def` | Reasonably foreseeable misuse definition specializing `DocumentedElement`. | `DocumentedElement` |
| [`RequirementKind`](#requirementkind) | `enum def` | The kind of requirement. Was a subclass branch (SystemRequirement / SoftwareRequirement / HardwareRequirement) plus a parallel design-control specification hierarchy (DesignControlSpecification / System- / Software- / HardwareSpecification); now one attribute.… | — |
| [`Requirement`](#requirement) | `requirement def` | Requirement definition specializing `VerifiableElement`. | `VerifiableElement` |
| [`SystemConstant`](#systemconstant) | `part def` | Named constant defined by a source requirements document (e.g. CriSys GPCA App. A.4) and referenced from requirement statements. | `MemoPart` |
| [`NotificationSpec`](#notificationspec) | `part def` | One row of a notification/alarm action table (e.g. CriSys GPCA REQ 59): a named notification, its priority, and the actions the system must take. | `MemoPart` |

## RiskDriver

```sysml
part def RiskDriver specializes RequirementDriver
```

| Property | Value |
| --- | --- |
| Description | Risk driver definition specializing `RequirementDriver`. |
| Kind | `part def` |
| Abstract | No |
| Specializes | `RequirementDriver` |
| Owning package | `memo_assurance_requirements` |


## IntendedUse

```sysml
part def IntendedUse specializes DocumentedElement
```

| Property | Value |
| --- | --- |
| Description | ISO 14971 risk-analysis inputs. These are model elements so hazards, scenarios, requirements, and DHF records can trace to the exact use definition or foreseeable misuse that motivated them. |
| Kind | `part def` |
| Abstract | No |
| Specializes | `DocumentedElement` |
| Owning package | `memo_assurance_requirements` |


## ReasonablyForeseeableMisuse

```sysml
part def ReasonablyForeseeableMisuse specializes DocumentedElement
```

| Property | Value |
| --- | --- |
| Description | Reasonably foreseeable misuse definition specializing `DocumentedElement`. |
| Kind | `part def` |
| Abstract | No |
| Specializes | `DocumentedElement` |
| Owning package | `memo_assurance_requirements` |


## RequirementKind

```sysml
enum def RequirementKind
```

| Property | Value |
| --- | --- |
| Description | The kind of requirement. Was a subclass branch (SystemRequirement / SoftwareRequirement / HardwareRequirement) plus a parallel design-control specification hierarchy (DesignControlSpecification / System- / Software- / HardwareSpecification); now one attribute.… |
| Kind | `enum def` |
| Abstract | No |
| Specializes | — |
| Owning package | `memo_assurance_requirements` |


## Requirement

```sysml
requirement def Requirement specializes VerifiableElement
```

| Property | Value |
| --- | --- |
| Description | Requirement definition specializing `VerifiableElement`. |
| Kind | `requirement def` |
| Abstract | No |
| Specializes | `VerifiableElement` |
| Owning package | `memo_assurance_requirements` |


## SystemConstant

```sysml
part def SystemConstant specializes MemoPart
```

| Property | Value |
| --- | --- |
| Description | Named constant defined by a source requirements document (e.g. CriSys GPCA App. A.4) and referenced from requirement statements. |
| Kind | `part def` |
| Abstract | No |
| Specializes | `MemoPart` |
| Owning package | `memo_assurance_requirements` |


## NotificationSpec

```sysml
part def NotificationSpec specializes MemoPart
```

| Property | Value |
| --- | --- |
| Description | One row of a notification/alarm action table (e.g. CriSys GPCA REQ 59): a named notification, its priority, and the actions the system must take. |
| Kind | `part def` |
| Abstract | No |
| Specializes | `MemoPart` |
| Owning package | `memo_assurance_requirements` |


## Source

??? code "assurance/requirements/memo_requirements.sysml"

    ```sysml
    package memo_assurance_requirements {
        private import ScalarValues::*;
    
        private import memo_core_common::*;
        private import memo_core_enumerations::*;
        // StakeholderNeed and the needs hierarchy now live in memo_assurance_needs.
        private import memo_assurance_needs::*;
        part def RiskDriver specializes RequirementDriver { attribute driverKind : String; }
    
        // ISO 14971 risk-analysis inputs. These are model elements so hazards,
        // scenarios, requirements, and DHF records can trace to the exact use
        // definition or foreseeable misuse that motivated them.
        part def IntendedUse specializes DocumentedElement {
            attribute indication : String;
            attribute contraindication : String;
            attribute patientPopulation : String;
            attribute clinicalBenefit : String;
            attribute regulatoryClassification : String;
        }
        part def ReasonablyForeseeableMisuse specializes DocumentedElement {
            attribute misuseContext : String;
            attribute foreseeableBasis : String;
            attribute affectedUseContext : String;
        }
    
        // The kind of requirement. Was a subclass branch (SystemRequirement /
        // SoftwareRequirement / HardwareRequirement) plus a parallel design-control
        // specification hierarchy (DesignControlSpecification / System- / Software- /
        // HardwareSpecification); now one attribute. The role-specific attributes
        // below are all optional so a requirement carries only the ones its kind
        // needs. IEC 62304 / design-control classification is preserved as the
        // enum value, not lost.
        enum def RequirementKind {
            enum system;
            enum software;
            enum hardware;
            enum systemSpecification;
            enum softwareSpecification;
            enum hardwareSpecification;
            enum designControl;
        }
    
        requirement def Requirement specializes VerifiableElement {
            attribute requirementKind : RequirementKind;
            attribute statement : String;
            attribute sourceKind : RequirementSourceKind;
            attribute concernKind : ConcernKind;
            attribute status : RequirementStatusKind;
            attribute acceptanceCriteria : String;
            attribute subjectDescription : String;
            attribute actorDescription : String;
            attribute assumptionSummary : String;
            attribute guaranteeSummary : String;
            // Authoring notation. `notation` selects the syntactic discipline the
            // `statement` follows; the slot attributes below carry the parsed clauses
            // so tools can lint pattern conformance and render either style.
            attribute notation : RequirementNotationKind;
            attribute earsPattern : EarsPatternKind;   // applies when notation = ears
            attribute obligation : ObligationKind;      // shall/should/will (RFC 2119, SOPHIST legal-obligation)
            attribute conditionClause : String;         // EARS When/While/If/Where or SOPHIST condition clause
            attribute systemResponse : String;          // the "shall <system response>" predicate
            // software requirement / specification (IEC 62304)
            attribute safetyClass : SafetyClassKind[0..1];
            // hardware requirement
            attribute electricalSafetyRelevant : Boolean;
            // design-control specification (ISO 13485 design output)
            attribute specificationDomain : String;
            attribute sourceFolder : String;
            attribute linkedUserNeeds : String;
            attribute linkedRisks : String;
            attribute linkedTests : String;
        }
        // Named constant defined by a source requirements document (e.g. CriSys
        // GPCA App. A.4) and referenced from requirement statements.
        part def SystemConstant specializes MemoPart {
            attribute symbol : String;          // identifier as written in the source, e.g. "Tolerance_Max const"
            attribute unit : String;
            attribute valueType : String;
            attribute constantValue : String;   // empty when the source leaves the value open
            attribute valueCondition : String;  // constraints on the value stated by the source
        }
        // One row of a notification/alarm action table (e.g. CriSys GPCA REQ 59):
        // a named notification, its priority, and the actions the system must take.
        part def NotificationSpec specializes MemoPart {
            attribute priority : NotificationPriorityKind;
            attribute visualNotification : Boolean;
            attribute audioNotification : Boolean;
            attribute inhibitInfusion : Boolean;
            attribute inhibitSquareBolus : Boolean;
            attribute inhibitPatientBolus : Boolean;
            attribute stopBolus : Boolean;
        }
    }
    
    ```
