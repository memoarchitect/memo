# Single-use device

A focused MEMO example for a single-use device.

- `model/single_use_device.sysml` is the public parent package.
- `model/catalog/` holds the canonical model elements.
- `model/viewpoints/` holds diagram views of that catalog.
