# Temperature alarm

A focused MEMO example for patient-temperature monitoring and alarming.

- `model/temperature_alarm.sysml` is the public parent package.
- `model/catalog/` holds the canonical model elements.
- `model/viewpoints/` holds diagram views of that catalog.
