# Functional, logical, physical

A focused MEMO example separating functional, logical, and physical design.

- `model/functional_logical_physical.sysml` is the public parent package.
- `model/catalog/` holds the canonical model elements.
- `model/viewpoints/` holds diagram views of that catalog.
