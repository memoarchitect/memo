# Multidimensional layers

A focused MEMO example showing layers and cross-cutting disciplines.

- `model/multidimensional_layers.sysml` is the public parent package.
- `model/catalog/` holds the canonical model elements.
- `model/viewpoints/` holds diagram views of that catalog.
