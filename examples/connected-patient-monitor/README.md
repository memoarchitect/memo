# Connected patient monitor

A focused MEMO example for a connected patient monitor.

- `model/connected_patient_monitor.sysml` is the public parent package.
- `model/catalog/` holds the canonical model elements.
- `model/viewpoints/` holds diagram views of that catalog.
