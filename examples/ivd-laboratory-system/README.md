# IVD laboratory system

A focused MEMO example for an IVD laboratory system.

- `model/ivd_laboratory_system.sysml` is the public parent package.
- `model/catalog/` holds the canonical model elements.
- `model/viewpoints/` holds diagram views of that catalog.
