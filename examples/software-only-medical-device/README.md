# Software-only medical device

A focused MEMO example for software without a hardware layer.

- `model/software_only_medical_device.sysml` is the public parent package.
- `model/catalog/` holds the canonical model elements.
- `model/viewpoints/` holds diagram views of that catalog.
