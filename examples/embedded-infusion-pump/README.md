# Embedded infusion pump

A focused MEMO example for an embedded infusion pump.

- `model/embedded_infusion_pump.sysml` is the public parent package.
- `model/catalog/` holds the canonical model elements.
- `model/viewpoints/` holds diagram views of that catalog.
