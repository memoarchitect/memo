# Reusable instrument

A focused MEMO example for a reusable medical instrument.

- `model/reusable_instrument.sysml` is the public parent package.
- `model/catalog/` holds the canonical model elements.
- `model/viewpoints/` holds diagram views of that catalog.
