# Manual surgical instrument

A focused MEMO example for a manual surgical instrument.

- `model/manual_surgical_instrument.sysml` is the public parent package.
- `model/catalog/` holds the canonical model elements.
- `model/viewpoints/` holds diagram views of that catalog.
